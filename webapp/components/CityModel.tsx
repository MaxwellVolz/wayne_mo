'use client'

/**
 * City Model component
 * Placeholder for imported Blender model with path vector points
 *
 * To use:
 * 1. Export your Blender model as .glb/.gltf
 * 2. Place it in /public/models/
 * 3. Use useGLTF from @react-three/drei to load it
 * 4. Extract path nodes from the model's named empty objects or vertices
 */
export default function CityModel() {
  // TODO: Import and render Blender model here
  // Example:
  // const { scene, nodes } = useGLTF('/models/city.glb')
  // return <primitive object={scene} />

  return null
}
